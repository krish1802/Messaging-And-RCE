##THIS IS ONLY FOR LINUX USERS

sudo apt-get install ssh

##Change the default IP, You can pass public IP and Port.

How To Use:-
1) Server.py is used for messaging purposes only, run the server.py on server machine so that the client can message about problems to the server
2) Command.py is used for RCE(Remote Code Execution) only, run the Command.py on server machine so that the client can give username and the server machine can access his/her linux operating system
3) Sender.py is used by the client , port 4444 is used for RCE and port 5555 is used for messaging the server, Changes can be made.

##CREATED BY KRISH KUMAR
